[pytest]
# Puts this directory (the one pytest.ini lives in) on sys.path so
# tests/test_*.py can `import persistent_tool_loop` etc. directly — same
# mechanism sdk/python/pyproject.toml uses for its own tests.
pythonpath = .
testpaths = tests
